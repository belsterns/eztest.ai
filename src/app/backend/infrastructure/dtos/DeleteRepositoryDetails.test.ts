import { DeleteRepositoryDetails } from './DeleteRepositoryDetails';

describe('DeleteRepositoryDetails', () => {
  it('should create an instance', () => {
    const instance = new DeleteRepositoryDetails();
    expect(instance).toBeInstanceOf(DeleteRepositoryDetails);
  });

  // Add more tests based on the methods and properties of DeleteRepositoryDetails
});